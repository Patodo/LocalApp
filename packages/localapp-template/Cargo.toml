[package]
name = "localapp-template"
version = "0.1.0"
edition = "2024"
license = "MIT"
build = "build.rs"

[dependencies]
include_dir = "0.7"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
tempfile = "3"
